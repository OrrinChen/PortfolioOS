# A-Share State-Transition Microstructure Family Closeout (2026-04-17)

## Final Classification

This primary family is closed for the current cycle as:

- `mechanism suggestive but unproven`

It is **not** a `family winner`.
It does **not** enter `D5 qualification handoff`.

## Why This Is The Final Classification

The family does not clear the charter's winner standard for this cycle for three separate reasons.

### 1. The upper-limit slice produced mixed, not portable, evidence

The first live upper-limit slice (`M1 / M2 / M5`, `P1 / P2 / P3 / P4`) was read on:

- `60` A-share growth names
- `553` trading dates
- `2024-01-02` through `2026-04-17`

That slice produced:

- two locally strong reads against the conditioned null and matched controls:
  - `P1_SEALED_UPPER_LIMIT`
  - `P4_NEXT_DAY_AFTER_FAILED`
- but all four first-wave expressions were weaker than their own pre-event placebo windows
- `M2` was weakened immediately because the failed-upper-limit branch did not land in the expected negative direction
- `M5` did not survive as a coherent cluster:
  - `P3` was negative
  - `P4` was positive but still placebo-contaminated

The slice-level closeout therefore remained:

- `mechanism suggestive but unproven`

Reference:

- [a_share_state_transition_upper_limit_slice_closeout_2026_04_17.md](C:\Users\14574\Quant\PortfolioOS\.worktrees\codex-us-alpha-week1-freeze\docs\strategy\a_share_state_transition_upper_limit_slice_closeout_2026_04_17.md)

### 2. The turnover-conditioned extension did not rescue the branch

An exploratory `M4` turnover-shock read was run on the same upper-limit event universe.

Result:

- sealed high-turnover events turned negative with null percentile `0.00`
- failed high-turnover events turned positive with null percentile `1.00`

This is the opposite of what a clean `M4` exhaustion / handoff story would need.
Therefore the current cycle did not justify opening a dedicated `M4` winner lane.

### 3. The remaining family branches could not be promoted honestly inside this cycle

#### Downside-state branch (`M3`)

On the live `2024 -> 2026` growth60 panel:

- lower-limit touched events = `7`

That is enough to show the branch exists.
It is not enough to support a serious winner claim.

#### Regime gate

The charter requires portable evidence across at least two institutional regimes.

Current live panel covers only:

- `R3 full-registration regime`

An attempt to unlock wider regime coverage through the current Tushare history path was made:

- `40` tickers
- `2020-01-01 -> 2026-04-17`
- provider-backed state-transition builder

Result:

- the build did not complete within a `20`-minute execution window
- no artifact was emitted

This does **not** prove the data are impossible to unlock.
It does prove that the current provider path is not operationally viable for this discovery cycle.

## Charter Consequence

Per the `D4` interpretation ladder:

- this family is above pure `no winner` because there are real, local, mechanism-linked reads worth preserving
- but it is below `regime-contingent local pattern`, because even the strongest branch is still placebo-contaminated and has not passed a second regime

Therefore the correct final classification is:

- `mechanism suggestive but unproven`

## What Is Preserved

This cycle still produced durable assets:

1. a validated discovery machine
   - calibration family completed
   - `P-001` exposure-conditioned null principle recorded
   - `FM-001` recorded and partially explained

2. a real provider-backed A-share state-transition panel path
   - Tushare historical builder
   - honest degraded-mode fallbacks
   - contract-shaped daily panel artifacts

3. a working upper-limit pilot chain
   - expression expansion
   - matched non-event controls
   - pre-event placebo
   - conditioned nulls
   - thin pilot artifact bundle

4. a bounded negative / mixed read on the first live family slice
   - this prevents future narrative inflation

## What Is Deferred Out Of Scope

This closeout does **not** claim:

- that the family is exhausted forever
- that `M3` has been disproved
- that a richer historical or intraday data path would not change the result

It only claims:

- under the current cycle's honest data and machine constraints,
- the family did **not** produce a promotable winner.

## Exit Rule

This closes the primary-family lane for the current charter cycle.

No further family mining is active under this charter unless a future program explicitly reopens one of:

1. a richer pre-`R3` historical state-transition data path
2. a deliberately downside-rich `M3` universe
3. a new charter with different scope or different data honesty boundaries
